#!/bin/sh

error() {
    MSG=$1; shift
    echo "$0: $MSG" 1>&2
    exit 1
}

PARS=$1; shift
[ "x$PARS" == "x" ] && PARS='parsec'
CPUS=2

cd "$PARS" || error "cannot find '$PARS'"
source ./env.sh || error "cannot find 'env.sh'"
parsecmgmt -a run -p streamcluster -n $CPUS -i simlarge
